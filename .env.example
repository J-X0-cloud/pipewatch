# Shared secret for run-event ingest (POST /api/webhooks/:source).
# Zapier, Make and n8n send it as the x-pipewatch-token header; custom webhooks use it
# to sign the body (x-pipewatch-signature: t=<unix>,v1=<hmac-sha256>).
PIPEWATCH_WEBHOOK_SECRET=
